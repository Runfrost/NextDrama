<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NextDrama.Views.MyPersonalPage">

    <!-- 🔹 HEADER - NextDrama + Tillbaka-knapp -->
    <Grid BackgroundColor="#695060" HeightRequest="60" Padding="10" Margin="0,50,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="1"
               Text="NextDrama"
               FontSize="30"
               FontAttributes="Bold"
               TextColor="White"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               FontFamily="OpenSansSemibold">
            <Label.Shadow>
                <Shadow Brush="Black" Offset="2,2" Opacity="0.6"/>
            </Label.Shadow>
        </Label>
    </Grid>

    <!-- 🔹 BANNER BILD -->
    <Image Source="header4.png"
           HeightRequest="330"
           Aspect="AspectFill"
           HorizontalOptions="Fill"
           VerticalOptions="Start"/>

</ContentPage>
